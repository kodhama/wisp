{
  "name": "grove-gate-profile",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gate-profile machinery (adr-0018): preset expansion, the intent-locus floor validator, and the load-time floor-guard with a guardian fallback. Zero-dependency, pure over explicit inputs.",
  "scripts": {
    "test": "node --test",
    "typecheck": "node --check lib/*.mjs && node --check bin/*.mjs && node --check test/*.mjs"
  }
}
